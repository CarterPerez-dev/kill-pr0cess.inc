/*
 * This file maps to the "/about" path in SolidStart's file-based routing system.
 */

import { Component } from 'solid-js';
import About from '../pages/About';

const AboutRoute: Component = () => {
  return <About />;
};

export default AboutRoute;
